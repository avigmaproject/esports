import { HomeState } from "./models";

const initialState: HomeState = {};

const reducer = (state: HomeState = initialState, action: any) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default reducer;
